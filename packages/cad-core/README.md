# CAD Core Package
Shared types and interfaces for handling 3D assembly data independent of Onshape-specific details. This package provides the abstract interface `CadProvider` that can be implemented by different backends (Onshape, mock, file import).

{
  "name": "onshape-3d-cad-visualizer",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test", 
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "turbo": "latest",
    "typescript": "^5.5.3"
  },
  "packageManager": "pnpm@9.6.0",
  "engines": {
    "node": ">=18.18"
  }
}
